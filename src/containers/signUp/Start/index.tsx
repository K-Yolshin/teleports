import React, { FC } from 'react';

import Message from '@/components/signUp/StartMessage';

/**
 * Container of the StartMessage.
 */
const Start: FC = () => (
  <Message to="./company" />
);

export default Start;
