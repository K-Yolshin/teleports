import styled from 'styled-components';

/*
 * Component of the MessagePage
 */
const MessagePage = styled.div`
  display: flex;
  justify-content: center;

  padding: 200px 0;
`;

export default MessagePage;
