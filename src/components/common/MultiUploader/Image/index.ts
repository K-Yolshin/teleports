import styled from 'styled-components';

import upload from '@/assets/addCompany/common/upload.svg';

/**
 * Component of the Image
 */
const Image = styled.div`
  width: 14px;
  height: 17px;

  margin-bottom: 9px;

  background: url(${upload}) no-repeat center;
`;

export default Image;
