import styled from 'styled-components';

/**
 * Component of the Item
 */
const Item = styled.div`
  flex: none;

  margin-right: 20px;
  margin-bottom: 20px;

  :last-of-type {
    margin-right: 0;
  }
`;

export default Item;
