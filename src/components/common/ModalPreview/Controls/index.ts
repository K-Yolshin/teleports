import styled from 'styled-components';

/**
 * Component of the Controls
 */
const Controls = styled.div`
  width: 172px;

  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  flex: none;

  padding: 25px;
`;

export default Controls;
