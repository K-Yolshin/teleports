import styled from 'styled-components';

/**
 * Component of the Root
 */
const Root = styled.div`
  display: flex;
  align-items: center;

  margin-bottom: 12px;

  :last-child {
    margin-bottom: 0;
  }
`;

export default Root;
