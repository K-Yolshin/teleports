import styled from 'styled-components';

/**
 * Component of the radio input DOM element.
 */
const InputComponent = styled.input`
  position: absolute;

  opacity: 0;
`;

export default InputComponent;
