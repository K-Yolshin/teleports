import styled from 'styled-components';

/**
 * Root element of the radio buttons.
 */
const Root = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
`;

export default Root;
