import styled from 'styled-components';

import Font from '@/helpers/Font';

/**
 * Helper text under the list of options.
 */
const HelperText = styled.div`
  font-family: ${Font.default};
  font-size: 14px;
  color: #9B9B9B;
  padding: 4px 11px;
`;

// Export.
export default HelperText;
