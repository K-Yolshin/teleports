import styled from 'styled-components';

import ImageBase from '../../PreviewImage';

/**
 * Component of the Image
 */
const Image = styled(ImageBase)`
  width: 100%;
  height: 100%;

  border-radius: 100%;
`;

export default Image;
