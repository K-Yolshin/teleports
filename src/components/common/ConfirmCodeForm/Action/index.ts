import styled from 'styled-components';

/**
 * Action element of the ConfirmCodeForm.
 */
const Action = styled.div`
  width: calc(50% - 5px);

  :first-child {
    margin-right: 5px;
  }

  :last-child {
    margin-left: 5px;
  }
`;

export default Action;
