import styled from 'styled-components';

/**
 * Inner element of the Profiles.
 */
const Inner = styled.div`
  display: flex;
  flex-wrap: wrap;

  margin-left: -7.5px;
  margin-right: -7.5px;
  margin-bottom: -15px;
`;

export default Inner;
