import styled from 'styled-components';

/**
 * Component of the Root for CardsGird.
 */
const Root = styled.div`
  display: flex;
  flex-flow: row wrap;

  margin-bottom: -15px;
`;

export default Root;
