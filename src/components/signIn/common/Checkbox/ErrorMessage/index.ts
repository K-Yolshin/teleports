import styled from 'styled-components';

/**
 * Component of the checkbox error message.
 */
const ErrorMessage = styled.div`
  margin-top: 5px;

  color: #D0021B;

  font-size: 12px;
  font-weight: 300;
`;

export default ErrorMessage;
