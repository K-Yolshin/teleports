import styled from 'styled-components';

/**
 * Base layout of the Select.
 */
const Root = styled.div`
  position: relative;

  display: flex;
  flex-direction: column;

  overflow: visible;
`;

export default Root;
