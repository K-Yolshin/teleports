import styled from 'styled-components';

import Root, { Props } from '../Root';

/**
 * LinkRoot element of the Button.
 */
const ButtonRoot = styled.button<Props>`${Root}`;

export default ButtonRoot;
export { Props };
